import { StyleSheet } from "react-native";

export const imageStyle = StyleSheet.create({

  welcomeImage: {
    width: 100,
    height: 80,
    resizeMode: "contain",
    marginTop: 3,
    marginLeft: -10
  },
});
